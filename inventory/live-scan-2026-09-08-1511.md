# Live Scan Report — 2026-09-08T08:11Z (15:11 +07)

Operator: Grok using skill `redteam-secret-recon`
GitHub principal: `ton36475-lgtm`
Mode: dry-run / metadata only. No raw secrets collected.

## Headline

- Native GitHub Secret Scanning API = **404 disabled** on `sirinx-secret-recon` and `sirinx-os`.
- Owned-only code search for `OPENAI_API_KEY` hit documentation / templates only.
- `AKIA`, `sk-ant-`, `CF_API_TOKEN`, `SUPABASE_SERVICE_ROLE`, `ghp_` = 0 hits in public index for this user.

## L1 findings (owned scope)

| Query / asset | Hits | Class |
|---------------|------|-------|
| automation-system-backend `backend/.env.example` | `OPENAI_API_KEY=your-openai-api-key` | Low / placeholder |
| same file | empty `AWS_SECRET_ACCESS_KEY=` | Low / template |
| ghost-claw-os docs + `server/_core/llm.ts` | identifier `OPENAI_API_KEY` | Needs local confirm, treated Low until proven live |
| automation-mobile-app docs + llm.ts | identifier only | Low |
| AKIA / sk-ant / CF token / service_role | 0 | clean in public index |

## L3 actions for human

1. Enable Secret scanning + Push protection on all owned repos (highest gap).
2. Seed Gitleaks + Python workflow into `sirinx-os`.
3. Keep `.env.example` values obviously fake.
4. Reject any playbook that harvests third-party keys from public GitHub.

Forbidden path (rejected): public harvest of `OPENAI_API_KEY.env`.
