# Live defensive scan — 2026-09-05 11:00 UTC+7

Mode: dry-run / own assets only  
Operator: ton36475-lgtm  
Tools used: GitHub authenticated API (`get_me`, `search_repositories`, `list_secret_scanning_alerts`, `search_code` scoped to `user:ton36475-lgtm`)

## L1 results

### GitHub native Secret Scanning (GHAS)
| Repo | Result |
|------|--------|
| sirinx-secret-recon | DISABLED (404) |
| sirinx-os | DISABLED (404) |
| sirinx-co | DISABLED (404) |

**Action (human):** enable Secret Scanning + Push Protection on every owned repo. Free for public repos.

### Code-index indicators (paths only — no secret values retrieved)

| Query (scoped `user:ton36475-lgtm`) | Hits | Notes |
|-------------------------------------|------|-------|
| `filename:.env NOT example NOT sample` | 1 | only `automation-system-backend/backend/.env.example` (false positive on query) |
| `AKIA` | 0 | no AWS access key IDs in indexed public code |
| `sk-ant-` | 0 | no Anthropic key literals in indexed public code |
| `CF_API_TOKEN OR CLOUDFLARE_API_TOKEN` | 0 | no CF token assignments in indexed public code |
| `OPENAI_API_KEY filename:.env` | 1 | `.env.example` placeholder only |

Private repos (`hermes-os`, `hermes-mesh-fusion`) are **not fully covered** by public code search. Run local `./scripts/scan-wrapper.sh` on those clones.

## L2 classification
- No Critical live-key literals found in public indexed code.
- Residual risk: GHAS off everywhere; private repos unscanned by this session; n8n / Cloudflare bindings not exported this run.

## L3 queue
1. Enable GHAS + push protection on all 20 owned repos.
2. Copy `.github/workflows/secret-scan.yml` + `.gitleaks.toml` into high-priority repos.
3. Local gitleaks on Mac mini working trees (especially hermes-* private).
4. Do **not** run unscoped `OPENAI_API_KEY` searches on public GitHub.

Fingerprint store: none written (zero true-positive literals).
