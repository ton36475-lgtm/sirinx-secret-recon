# Live scan — 2026-09-06 17:05 +07 (10:06 UTC)

Skill: `redteam-secret-recon`  
Operator: `ton36475-lgtm`  
Mode: dry-run, owned assets only

## Rejected request

Telegram screenshots asked to search public GitHub for `OPENAI_API_KEY` / `OPENAI_API_KEY.env` to use other people’s keys (“Vibecode ไม่ต้องเขียนโปรแกรม”).  
**Not executed.** Policy + provider TOS + computer-misuse risk.

## L1

### Identity
- GitHub login `ton36475-lgtm` (uid 270072061), 20 repos.

### Native secret scanning
| Repo | API |
|------|-----|
| sirinx-co | 404 disabled |
| sirinx-os | 404 disabled |
| sirinx-secret-recon | 404 disabled |
| sirinx-skills-kit | OK, 0 open alerts |

### Owned-account code search (`user:ton36475-lgtm` only)
- `filename:.env` → `automation-system-backend/backend/.env.example` only
- `OPENAI_API_KEY` → 8 hits, all docs / `llm.ts` / `.env.example` / docker-compose (env names)
- `AKIA` / `sk-ant-` / `CF_API_TOKEN` → 0

Local package scan: 2 Low placeholders in `fixtures/.env.example` only. Skill tree: 0.

## L2

Placeholders → Low. GHAS disabled on most of fleet → High control gap. No live production keys in indexed public owned trees.

## L3 human gate

1. Enable Secret scanning + Push protection (`docs/ENABLE_GHAS.md`).
2. Copy workflow + `.gitleaks.toml` into remaining targets.
3. Deploy schema / n8n in dry-run.
4. Do not implement public key harvesting. Do not auto-rotate.

No raw secret values in this file.
