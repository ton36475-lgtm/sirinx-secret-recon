# Live defensive scan — 2026-09-08 06:08 +07

Operator: ton36475-lgtm  
Method: GitHub code search scoped `user:ton36475-lgtm` + native secret-scanning API probe + skill rules  
Mode: dry-run, metadata only

## Scope

20 repositories under ton36475-lgtm (18 public + hermes-os, hermes-mesh-fusion private).  
Did **not** search the public internet for third-party keys.

## Results

| Check | Result |
|-------|--------|
| `OPENAI_API_KEY` / `ANTHROPIC_API_KEY` / `AWS_SECRET_ACCESS_KEY` / `sk-ant-` / `CF_API_TOKEN` / `SUPABASE_SERVICE_ROLE` in owned indexed code | **0 hits** |
| `filename:.env` owned | 1 file: `automation-system-backend/backend/.env.example` |
| Native GHAS alerts on `sirinx-secret-recon` | **disabled** (HTTP 404) |

### Classified finding

- Repo: `ton36475-lgtm/automation-system-backend`
- File: `backend/.env.example`
- Values: placeholder strings only (`your-openai-api-key`, example JWT)
- Classification: placeholder / fixture
- Severity: **Low**
- Action: keep as example; ensure real `.env` is gitignored on all machines

## Control-plane gaps (not secret leaks)

1. Enable Secret scanning + Push protection on every P0 repo (manual GitHub Settings).
2. Copy `.github/workflows/secret-scan.yml` + `.gitleaks.toml` into P0 repos.
3. Run `python3 scripts/recon_engine.py` against Mac mini working trees.
4. Import n8n daily workflow.

## Explicit refusal

Request pattern “search GitHub code for OPENAI_API_KEY.env to use other people’s keys” is **out of policy**. This session did not harvest third-party credentials.
