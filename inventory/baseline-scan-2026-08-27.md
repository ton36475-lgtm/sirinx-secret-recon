# Baseline scan report — 2026-08-27

Scope: `user:ton36475-lgtm` only. No public harvest.

- Secret scanning disabled on `sirinx-co` (API 404).
- Owned-repo hits for OPENAI_API_KEY are docs / env.example / llm.ts paths only (no raw values logged).
