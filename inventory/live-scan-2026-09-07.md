# Live defensive scan — 2026-09-07 05:10 +07

Scope: `user:ton36475-lgtm` only. No public-web / third-party harvest.

## GitHub native Secret Scanning (GHAS)

| Repo | Result |
|------|--------|
| sirinx-secret-recon | 404 — secret scanning **disabled** |
| sirinx-os | 404 — secret scanning **disabled** |
| sirinx-co | 404 — secret scanning **disabled** |

Action (human): enable Secret scanning + Push protection on every public repo.

## Code-search (paths and filenames only — values not retrieved)

| Query (scoped `user:ton36475-lgtm`) | Hits |
|-------------------------------------|------|
| `filename:.env` | 1 — `automation-system-backend/backend/.env.example` (example only) |
| `OPENAI_API_KEY` | 8 — env-var **names** in docs/code. No committed live key observed. |
| `AKIA` | 0 |
| `sk-ant-` | 0 |
| `CF_API_TOKEN` / `SUPABASE_SERVICE_ROLE` | 0 |
| `filename:id_rsa` / `credentials.json` / `.npmrc` | 0 |

## Classification (L2, dry-run)

| Finding | Severity | Verdict |
|---------|----------|---------|
| `.env.example` present | Low / Info | Expected template. |
| `OPENAI_API_KEY` identifier in source/docs | Low / Info | Name-only references. |
| GHAS disabled fleet-wide | High (control gap) | Enable scanning + push protection. |

## What was not done (policy)

- No unscoped `OPENAI_API_KEY` search across all of GitHub.
- No raw secret values printed or stored.
- No key validation against providers.
- No rotation.
