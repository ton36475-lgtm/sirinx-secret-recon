# Live scan 2026-09-04 04:08 +07

Skill: redteam-secret-recon
Operator: ton36475-lgtm
Mode: dry-run
Scope: owned assets only

## Rejected request from Telegram screenshots

Search public GitHub for `OPENAI_API_KEY` / `OPENAI_API_KEY.env` to use other people’s keys / Vibecode — **denied**.

## L1

Native secret scanning:

| Repo | API |
|---|---|
| sirinx-skills-kit | ON, 0 open alerts |
| sirinx-secret-recon | OFF (404) |
| sirinx-os | OFF (404) |
| sirinx-co | OFF (404) |
| ghost-claw-os | OFF (404) |
| hermes-os | OFF (404) |
| hermes-mesh-fusion | OFF (404) |

Owned-account code search (`user:ton36475-lgtm` only):

- `OPENAI_API_KEY` → 8 hits in docs / `llm.ts` / `.env.example` / docker-compose
- `filename:.env` → `automation-system-backend/backend/.env.example` only
- `AKIA` / `sk-ant-` / `sk-proj-` / `CF_API_TOKEN` / `service_role` → 0

`.env.example` values are placeholders (`your-openai-api-key`, empty AWS keys, example JWT string).

## L2

No live production key material in indexed owned public trees.
Highest real finding: **control gap** — GHAS + push protection off on most of the fleet.

## L3 human gate

1. Enable Secret scanning + Push protection (`docs/ENABLE_GHAS.md`).
2. Copy workflow + `.gitleaks.toml` into remaining target repos.
3. Import n8n orchestrator in dry-run; apply `supabase/schema.sql`.
4. Do not implement public harvest. Do not auto-rotate.

No raw secret values in this report.
