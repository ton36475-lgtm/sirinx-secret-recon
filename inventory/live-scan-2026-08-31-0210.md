# Live Scan Inventory — 2026-08-31 02:10 (+07)

**Scope**: Authenticated user `ton36475-lgtm` owned assets only (SIRINX / GhostClaw). Defensive posture. No third-party key harvesting.

## Summary

| Metric | Value |
|--------|-------|
| Owned repos (public + private visible) | 19 |
| GHAS Secret Scanning enabled (confirmed) | 1 (`sirinx-skills-kit`) |
| GHAS disabled / 404 on priority set | Majority (sirinx-os, sirinx-co, oz-corp-omega-dual-node, ghost-claw-os, hermes-os, automated-marketing-agency, sirinx-secret-recon, …) |
| Open GHAS secret-scanning alerts | 0 |
| Code-search hits (user:ton36475-lgtm) for `.env` / high-signal | 0 live secrets; only documentation / error-string references to `OPENAI_API_KEY` / `api_key` env vars |
| High / Critical true positives (own repos) | **0** |
| Raw secret values printed or stored | **None** |

## Priority repos checked (GHAS list_secret_scanning_alerts)

- `sirinx-skills-kit`: enabled, open alerts = []
- `sirinx-os`, `sirinx-co`, `sirinx-secret-recon`, `oz-corp-omega-dual-node`, `ghost-claw-os`, `hermes-os`, `automated-marketing-agency`: Secret scanning disabled (404)

## Fallback code search (scoped `user:ton36475-lgtm` only)

- `filename:.env` / `filename:.env*`: 0 results
- `OPENAI_API_KEY`: 2 files (ghost-claw-os, automation-mobile-app) — error message strings only (`throw new Error("OPENAI_API_KEY is not configured")`)
- `sk-`, `ANTHROPIC`, high-signal patterns: no live key material returned
- `api_key` / `API_KEY`: env-handling / README references only

**Classification**: All observed matches are placeholders / documentation / runtime-config references. No live production keys exposed in indexed content.

## Recommended human-gate actions

1. Enable GitHub Advanced Security / Secret Scanning + Push Protection on all remaining owned repositories (especially private `hermes-os` and high-activity SIRINX monorepos).
2. Add pre-commit / CI gitleaks (see repo `.gitleaks.toml` and `scripts/`) for custom patterns and full history.
3. Confirm no local / n8n / Cloudflare Worker env exports contain unmasked secrets outside secret managers.
4. Re-scan after enabling GHAS.

## Operator notes

- No issue opened on this run (no new High/Critical true positives).
- This file contains metadata only; no secret values.
