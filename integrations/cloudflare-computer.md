# Cloudflare Computer as an L1 runner

Reference: https://github.com/cloudflare/computer
Use as a sandboxed workspace for scanner sub-agents. Never harvest third-party keys. Never persist raw secrets in the VFS.
